diff --git a/node_modules/react-native/Libraries/PushNotificationIOS/PushNotificationIOS.js b/node_modules/react-native/Libraries/PushNotificationIOS/PushNotificationIOS.js
index 2e71f02..3106159 100644
--- a/node_modules/react-native/Libraries/PushNotificationIOS/PushNotificationIOS.js
+++ b/node_modules/react-native/Libraries/PushNotificationIOS/PushNotificationIOS.js
@@ -11,6 +11,7 @@
 import type {EventSubscription} from '../vendor/emitter/EventEmitter';
 
 import NativeEventEmitter from '../EventEmitter/NativeEventEmitter';
+import RCTDeviceEventEmitter from '../EventEmitter/RCTDeviceEventEmitter';
 import Platform from '../Utilities/Platform';
 import NativePushNotificationManagerIOS from './NativePushNotificationManagerIOS';
 import invariant from 'invariant';
@@ -63,12 +64,13 @@ type NativePushNotificationIOSEventDefinitions = {
   localNotificationReceived: [{...}],
 };
 
+// Expo / expo-notifications: legacy PushNotificationManager turbo module is often absent.
 const PushNotificationEmitter =
-  new NativeEventEmitter<NativePushNotificationIOSEventDefinitions>(
-    // T88715063: NativeEventEmitter only used this parameter on iOS. Now it uses it on all platforms, so this code was modified automatically to preserve its behavior
-    // If you want to use the native module on other platforms, please remove this condition and test its behavior
-    Platform.OS !== 'ios' ? null : NativePushNotificationManagerIOS,
-  );
+  Platform.OS === 'ios' && NativePushNotificationManagerIOS != null
+    ? new NativeEventEmitter<NativePushNotificationIOSEventDefinitions>(
+        NativePushNotificationManagerIOS,
+      )
+    : RCTDeviceEventEmitter;
 
 const _notifHandlers = new Map<string, void | EventSubscription>();
 
