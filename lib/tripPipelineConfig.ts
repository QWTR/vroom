/**
 * Jeden przełącznik trybu jazdy/nawigacji.
 * SIMPLE=true: marker + snap + kamera + prędkość — priorytet działania nad anty-teleport.
 * Wyłącz dopiero gdy pole jest stabilne i wracamy ostre guardy.
 */
export const TRIP_PIPELINE_SIMPLE = true;
