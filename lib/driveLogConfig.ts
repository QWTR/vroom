/** Pełny trace jazdy → logcat [VROOM-TEL] + plik Download/vroom_drive_session.log */
export const DRIVE_SESSION_TRACE_ENABLED = true;

/** MAX diagnostyka na test w aucie — klatki markera/kamery co 500ms, eventy bez throttle. */
export const DRIVE_FULL_VISION_LOG = true;
