// Re-export everything from the shared SettingsContext so existing imports work unchanged.
export type { AppSettings } from '../contexts/SettingsContext';
export { useSettings } from '../contexts/SettingsContext';
