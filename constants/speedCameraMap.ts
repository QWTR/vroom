/** Minimalny zoom — poniżej warstwa radarów jest ukryta (jak stacje paliw). */
export const SPEED_CAMERA_MIN_ZOOM = 11.5;

/** Klucz tekstury w atlasie Mapbox.Images (Mapbox.Image name). */
export const SPEED_CAMERA_MAP_ICON = 'speed_camera_icon';

/** Rozmiar sprite'a React (px) — większy = czytelniejszy pin na mapie. */
export const SPEED_CAMERA_SPRITE_PX = 35;

/** Rozmiar ikony na mapie — mnożnik tekstury Mapbox. */
export const SPEED_CAMERA_ICON_SIZE = 1.08;
