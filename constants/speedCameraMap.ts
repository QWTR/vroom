/** Minimalny zoom — poniżej warstwa radarów jest ukryta (jak stacje paliw). */
export const SPEED_CAMERA_MIN_ZOOM = 11.5;

/** Klucz tekstury w atlasie Mapbox.Images (Mapbox.Image name). */
export const SPEED_CAMERA_MAP_ICON = 'speed_camera_icon';

/** Rozmiar ikony na mapie — View 32×32 px, skala 1 = natywny rozmiar. */
export const SPEED_CAMERA_ICON_SIZE = 1;

/** Nieco większa ikona dla klastrów (licznik na środku). */
export const SPEED_CAMERA_CLUSTER_ICON_SIZE = 1.1;

/** Mapbox clusterMaxZoomLevel — powyżej tego zoomu pojedyncze piny zamiast klastrów. */
export const SPEED_CAMERA_CLUSTER_MAX_ZOOM = 14;

export const SPEED_CAMERA_CLUSTER_RADIUS = 48;
